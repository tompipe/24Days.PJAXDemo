@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@using Newtonsoft.Json.Linq

@{
    if (Model.Content.HasValue("highlights") && Model.Content.GetPropertyValue<string>("highlights").Length > 2)
    {
        <h2>@Model.Content.GetPropertyValue("highlightsTitle", "Highlights")</h2>
    
        @*<ul>
            @foreach (var item in CurrentPage.highlights)
            {
                var linkUrl = (bool)item.isInternal ? Umbraco.NiceUrl(item.Value<int>("internal")) : item.link;
                var linkTarget = (bool)item.newWindow ? "_blank" : null;
                <li><a href="@linkUrl" target="@linkTarget">@item.caption</a></li>
            }
        </ul>*@
    }
}

